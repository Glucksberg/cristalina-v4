{
  "name": "@cristalina-v4/hermes-adapter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Hermes Agent integration for Cristalina v4",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @cristalina-v4/core build && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@cristalina-v4/core": "workspace:*"
  }
}
